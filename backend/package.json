{
  "name": "hmb-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "func start",
    "build": "echo 'No build necessary for backend'; exit 0",
    "bundle": "esbuild index.js --bundle --platform=node --target=node18 --outfile=dist/index.js"
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "exifr": "^7.1.0",
    "file-type": "^20.5.0",
    "formidable": "^3.5.2",
    "sharp": "^0.32.0",
    "streamifier": "^0.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
